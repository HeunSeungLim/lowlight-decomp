조판되지 않는 옛 표 행 파일이다. 원고가 인쇄하지 않으며 값이 현재 정본과 어긋나는 것이 있다
(tab_anc_rows 의 24.84, tab_cal_rows 의 24.82 는 표 3 의 24.64 와 다른 옛 규칙 산출값이다).
생성기는 아직 이 파일들을 만들고, 감사는 여기 있는 사본을 생성기 출력과 계속 대조한다.
다만 이 값들이 감사의 수용 집합을 넓히지는 않는다 — 24.82/24.84 는 프레임별 덤프에서 오지 이 tex 에서 오지 않는다.
